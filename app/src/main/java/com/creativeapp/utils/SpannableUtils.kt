package com.creativeapp.utils

import android.widget.TextView


class SpannableUtils {

    fun SpanColoredTwoSectors(textView: TextView,
                              firstText: String,
                              firstColorId: Int,
                              secondText: String,
                              secondColorId: Int) {
       // val allText = "$firstText $secondText"




//        wordSpan.setSpan(ForegroundColorSpan(Utils.(textView.getContext(), firstColorId)), 0, allText.indexOf(secondText) - 1, Spannable.SPAN_EXCLUSIVE_EXCLUSIVE)
//        wordSpan.setSpan(ForegroundColorSpan(Utils.getColor(textView.getContext(), secondColorId)), allText.indexOf(secondText), allText.length + 1, Spannable.SPAN_INCLUSIVE_INCLUSIVE)
//        textView.setText(wordSpan, TextView.BufferType.SPANNABLE)
    }
}